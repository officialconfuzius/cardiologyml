from pathlib import Path

# Define the base directory, necessary on windows machines
BASE_DIR = Path(__file__).resolve().parent

# Define the path to the segmentation data
DATA_PATH = "../SegmentationsPablo"
